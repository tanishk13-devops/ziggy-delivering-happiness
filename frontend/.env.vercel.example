# Set this variable in Vercel project settings
# Single-URL setup (recommended): keep frontend calls same-origin
# and add Vercel rewrite /api/:path* -> https://<render-api>.onrender.com/api/:path*
NG_APP_API_URL=/api
